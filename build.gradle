group 'ml.stephen'
version '1.0.1'

apply plugin: 'java'
apply plugin: 'war'

apply from: 'libraries.gradle' /** jar */
apply from: 'tasks.gradle' /** task */

/** jdk版本 */
sourceCompatibility = 1.8

/** war包名称 */
archivesBaseName = 'ROOT'

repositories {
    mavenCentral()
}